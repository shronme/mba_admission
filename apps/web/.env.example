# Public: used in the browser to call the FastAPI backend.
# Must be the **API** service URL (where /health lives), not the Next.js frontend URL.
# Production example: https://your-api-service.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8000
